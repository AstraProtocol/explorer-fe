describe('Render Address Detail Tab', () => {
	const HOST = 'http://localhost:3000/tx'
	beforeEach(() => {
		cy.viewport('macbook-13')
		cy.visit(HOST)
	})

	describe('render tab transactions', () => {})
	describe('render tab token transfers', () => {})
	describe('render tab tokens', () => {})
	describe('render tab internal tx', () => {})
	describe('render tab coin balance history', () => {})
	describe('render tab contract', () => {})
})
